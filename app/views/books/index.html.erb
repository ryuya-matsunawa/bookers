<div>
    <table>
        <% @books.each do |book| %>
            <tr>
                <td><% book.title %></td>
                <td><% book.body %></td>
            </tr>
        <% end %>
    </table>
</div>